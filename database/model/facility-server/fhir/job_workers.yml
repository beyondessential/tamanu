version: 2
sources:
  - name: tamanu
    schema: fhir
    description: TODO
    __generator:
      js_class: JobWorker
    tables:
      - name: job_workers
        description: "{{ doc('table__job_workers') }}"
        tags: []
        columns:
          - name: id
            data_type: uuid
            description: "{{ doc('job_workers__id') }}"
            data_tests:
              - unique
              - not_null
          - name: created_at
            data_type: timestamp with time zone
            description: "{{ doc('job_workers__created_at') }}"
            data_tests:
              - not_null
          - name: updated_at
            data_type: timestamp with time zone
            description: "{{ doc('job_workers__updated_at') }}"
            data_tests:
              - not_null
          - name: deleted_at
            data_type: timestamp with time zone
            description: "{{ doc('job_workers__deleted_at') }}"
          - name: metadata
            data_type: jsonb
            description: "{{ doc('job_workers__metadata') }}"
            data_tests:
              - not_null
