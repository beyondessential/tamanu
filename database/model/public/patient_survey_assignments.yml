version: 2
sources:
  - name: tamanu
    schema: public
    description: "{{ doc('generic__schema') }}"
    tables:
      - name: patient_survey_assignments
        description: '{{ doc("table__patient_survey_assignments") }}'
        config:
          tags: []
        columns:
          - name: id
            data_type: text
            description: "{{ doc('generic__id') }} in patient_survey_assignments."
            data_tests:
              - unique
              - not_null
          - name: created_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__created_at') }} in patient_survey_assignments."
            data_tests:
              - not_null
          - name: updated_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__updated_at') }} in patient_survey_assignments."
            data_tests:
              - not_null
          - name: deleted_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__deleted_at') }} in patient_survey_assignments."
          - name: patient_id
            data_type: text
            description: "{{ doc('patient_survey_assignments__patient_id') }}"
            data_tests:
              - not_null
          - name: survey_id
            data_type: text
            description: "{{ doc('patient_survey_assignments__survey_id') }}"
            data_tests:
              - not_null
          - name: status
            data_type: text
            description: "{{ doc('patient_survey_assignments__status') }}"
            data_tests:
              - not_null
          - name: completed_at
            data_type: character(19)
            description: "{{ doc('patient_survey_assignments__completed_at') }}"
          - name: assigned_by_id
            data_type: text
            description: "{{ doc('patient_survey_assignments__assigned_by_id') }}"
            data_tests:
              - not_null
          - name: survey_response_id
            data_type: text
            description: "{{ doc('patient_survey_assignments__survey_response_id') }}"
          - name: updated_at_sync_tick
            data_type: bigint
            description: "{{ doc('generic__updated_at_sync_tick') }} in patient_survey_assignments."
            data_tests:
              - not_null
          - name: assigned_at
            data_type: character(19)
            description: "{{ doc('patient_survey_assignments__assigned_at') }}"
            data_tests:
              - not_null
