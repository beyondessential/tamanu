version: 2
sources:
  - name: tamanu
    schema: public
    description: "{{ doc('generic__schema') }}"
    tables:
      - name: patient_users
        description: "{{ doc('table__patient_users') }}"
        config:
          tags: []
        columns:
          - name: id
            data_type: text
            description: "{{ doc('generic__id') }} in patient_users."
            data_tests:
              - unique
              - not_null
          - name: created_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__created_at') }} in patient_users."
            data_tests:
              - not_null
          - name: updated_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__updated_at') }} in patient_users."
            data_tests:
              - not_null
          - name: deleted_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__deleted_at') }} in patient_users."
          - name: patient_id
            data_type: text
            description: "{{ doc('patient_users__patient_id') }}"
            data_tests:
              - not_null
          - name: role
            data_type: text
            description: "{{ doc('patient_users__role') }}"
            data_tests:
              - not_null
          - name: email
            data_type: text
            description: "{{ doc('patient_users__email') }}"
            data_tests:
              - unique
          - name: visibility_status
            data_type: text
            description: "{{ doc('generic__visibility_status') }} in patient_users."
          - name: status
            data_type: text
            description: "{{ doc('patient_users__status') }}"
            data_tests:
              - not_null
          - name: updated_at_sync_tick
            data_type: bigint
            description: "{{ doc('generic__updated_at_sync_tick') }} in patient_users."
            data_tests:
              - not_null
