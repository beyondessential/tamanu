---
name: git-update
description: "Rebase a card's branch onto its upstream (parent card branch or main) and check for both hard and soft conflicts. Use when asked to 'update from upstream', 'rebase onto main', or 'sync the card branch'."
model: inherit
---

Read `.agents/skills/git-update.md` and follow the task described there.
