---
name: draft-spec-changes
description: "Produce spec edits directly from a card description following `.agents/docs/spec-format.md`, defaulting to editing existing specs rather than creating new ones. Use when asked to 'draft specs', 'write spec changes', or to capture a card's requirements as spec edits."
model: inherit
---

Read `.agents/skills/draft-spec-changes.md` and follow the task described there.
