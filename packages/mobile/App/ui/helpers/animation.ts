// Reanimated v1 imperative API (Clock, block, cond, Value, etc.) was removed in v3.
// Animations that previously lived here have been inlined into their call sites
// using the v3 hooks API (useSharedValue / withTiming / withRepeat / useAnimatedStyle).
