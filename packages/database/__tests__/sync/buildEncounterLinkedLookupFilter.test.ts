import { describe } from 'vitest';

describe('buildEncounterLinkedLookupFilter', () => {
  test.todo('should include facility filter if provided');
  test.todo('should not include facility filter if not provided');
});
