import { test } from '../../fixtures/baseFixture';

test.describe('Patient Side Bar', () => {
  test('Add ongoing condition', async ({ page }) => {});

  test.describe('Death workflow', () => {
    test('Record death - Male, 3 months', async ({ page }) => {});
  });
});
