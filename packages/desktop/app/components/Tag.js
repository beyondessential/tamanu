import styled from 'styled-components';

export const Tag = styled.div`
  background: ${p => p.$background};
  color: ${p => p.$color};
  padding: 3px 13px;
  border-radius: 20px;
  font-weight: 400;
  font-size: 14px;
  line-height: 18px;
`;
