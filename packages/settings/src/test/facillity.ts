export const facilityTestSettings = {
  log: {
    consoleLevel: null,
    enableAuditLog: true,
  },
  sync: {
    backoff: {
      maxAttempts: 1,
      multiplierMs: 0,
      maxWaitMs: 0,
    },
  },
};
