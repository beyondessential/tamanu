{
  "name": "@tamanu/new-package",
  "version": "0.0.0",
  "private": true,
  "description": "TODO",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "homepage": "https://github.com/beyondessential/tamanu.git#readme",
  "repository": "git@github.com:beyondessential/tamanu.git",
  "author": "Beyond Essential Systems Pty. Ltd.",
  "license": "SEE LICENSE IN ../../license",
  "scripts": {
    "build": "yarn run build:src && yarn run build:types",
    "build:src": "swc --delete-dir-on-start --out-dir dist --copy-files --source-maps true src",
    "build:types": "tsc --outDir dist --declaration --emitDeclarationOnly --noEmit false && mv dist/src/* dist && rm -rf dist/src",
    "build-watch": "yarn run build:src --watch",
    "test": "NODE_ENV=test jest",
    "lint": "yarn run lint:check && yarn run lint:types",
    "lint:check": "rome check src",
    "lint:types": "tsc",
    "lint:fix": "rome check src --apply-unsafe",
    "fmt": "rome format src --write"
  },
  "devDependencies": {
    "@tamanu/build-tooling": "*",
    "@jest/globals": "^29.4.3",
    "@swc/jest": "^0.2.24",
    "@types/node": "^18.14.6",
    "jest": "^29.4.3"
  },
  "dependencies": {
  }
}
