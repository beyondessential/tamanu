import { Box, Typography } from '@mui/material';

export const PageHeader = () => {
  return (
    <Box sx={{ backgroundColor: 'background.paper', p: 2 }}>
      <Typography variant="h1">tamanu</Typography>
    </Box>
  );
};
