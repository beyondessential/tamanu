name: Review Hero Auto-Fix
# NOTE: disabled to workflow_dispatch-only on the experimental branch
# claude/windows-vhdx-oci-artifact-f6qhdy to avoid unneeded GHA churn.
on:
  workflow_dispatch:

permissions:
  actions: read
  contents: write
  pull-requests: write

jobs:
  auto-fix:
    uses: beyondessential/review-hero/.github/workflows/auto-fix.yml@v1
    secrets: inherit
