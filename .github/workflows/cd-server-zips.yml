name: CD Server Zips

on:
  workflow_dispatch:
  push:
    tags:
      - v*.*.*

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  id-token: write # OIDC token for AWS

jobs:
  info:
    name: Branch info
    uses: ./.github/workflows/branch-info.yml

  image:
    needs: info
    uses: ./.github/workflows/build-image.yml
    strategy:
      matrix:
        package:
          - name: central
            path: sync-server
          - name: facility
            path: lan
    with:
      package-name: ${{ matrix.package.name }}
      package-path: ${{ matrix.package.path }}
      image-shatag: false
      image-tags: ${{ needs.info.outputs.version }}-serverzips

  build:
    needs:
      - info
      - image
    strategy:
      fail-fast: false
      matrix:
        package:
          - name: central
            path: sync-server
          - name: facility
            path: lan

    name: Build ${{ matrix.package.name }} server zip
    runs-on: windows-latest
    env:
      OUTPUT_NAME: "${{ matrix.package.name }}-${{ needs.info.outputs.version }}-windows"
      S3_PATH: s3://bes-tamanu-release-servers/${{ needs.info.outputs.version }}

    steps:
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-region: ap-southeast-2
          role-to-assume: arn:aws:iam::143295493206:role/gha-image-pull
          role-session-name: GHA@Tamanu=ServerZips
          mask-aws-account-id: false

      # This is a workaround for https://github.com/aws-actions/amazon-ecr-login not working on windows
      # Root cause seems to be https://github.com/docker/docker-credential-helpers/issues/190
      - name: Login to ECR
        shell: bash
        run: |
          echo '{"auths": {"https://index.docker.io/v1/": {}}, "HttpHeaders": { "User-Agent": "Docker-Client/24.0.2 (windows)"}}' > ~/.docker/config.json
          aws ecr get-login-password --region ap-southeast-2 | docker login --username AWS --password-stdin ${{ needs.image.outputs.registry }}

      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: yarn

      - name: Create container
        run: docker create --name working ${{ needs.image.outputs.registry }}/tamanu/${{ matrix.package.name }}:${{ needs.info.outputs.version }}-serverzips

      - name: Copy files
        shell: bash
        run: docker cp working:/app $OUTPUT_NAME

      - name: Reinstall dependencies
        working-directory: ${{ env.OUTPUT_NAME }}
        shell: bash
        run: |
          set -ex
          rm -rf node_modules packages/*/node_modules
          yarn install --frozen-lockfile --production

      - name: Delete container
        run: docker rm working

      - name: Prepare production configuration
        shell: bash
        run: |
          set -ex
          rm packages/${{ matrix.package.path }}/config/local.json
          sed -E \
            "s|cwd: '.',( //.*)?|cwd: 'packages/${{ matrix.package.path }}',|" \
            packages/${{ matrix.package.path }}/pm2.config.js > ${{ env.OUTPUT_NAME }}/pm2.config.js

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: debug
          path: '${{ env.OUTPUT_NAME }}'

      - name: Prepare final output
        run: zip -r '${{ env.OUTPUT_NAME }}.zip' '${{ env.OUTPUT_NAME }}'

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-region: ap-southeast-2
          role-to-assume: arn:aws:iam::143295493206:role/gha-server-s3
          role-session-name: GHA@Tamanu=ServerZips
          mask-aws-account-id: false

      - name: Push to S3
        run: aws s3 cp '${{ env.OUTPUT_NAME }}.zip' '${{ env.S3_PATH }}/${{ env.OUTPUT_NAME }}.zip' --no-progress
