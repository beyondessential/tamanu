# Agent: Security

Focus: SQL injection (especially raw `sequelize.query()` with interpolation), XSS, auth bypass, sensitive data exposure, input validation at system boundaries, path traversal, SSRF, hardcoded secrets, CSRF.

Ignore: performance, style, BES conventions, correctness, theoretical vulnerabilities requiring already-compromised systems.
