name: Setup Android ccache
description: Install ccache for React Native Android native builds

runs:
  using: composite
  steps:
    - name: Install ccache
      shell: bash
      run: sudo apt-get update && sudo apt-get install -y ccache
