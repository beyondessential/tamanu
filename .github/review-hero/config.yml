project: Tamanu
description: a healthcare management system

agents:
  bes-requirements:
    name: "BES Requirements"
    description: "Tamanu conventions, migrations, FHIR, sync, Australian English, TranslatedText, permissions"
  endpoint-tests:
    name: "Integration tests"
    description: "New or updated API endpoints in central-server or facility-server that are missing corresponding integration tests (supertest, createTestContext, auth coverage)"
  e2e-tests:
    name: "E2e tests"
    description: "New or updated user-facing features, pages, or clinical flows that are missing corresponding Playwright E2E tests covering the happy path"
