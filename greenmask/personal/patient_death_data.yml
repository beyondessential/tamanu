---
schema: public
name: patient_death_data
transformers:
  - name: RealAddress
    params:
      columns:
        - name: external_cause_location
          template: '{{.Address}} {{.City}}'
          keep_null: true
