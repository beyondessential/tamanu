app:
  build: .
  encrypted_env_file: codeship.env.encrypted
  volumes:
    - ./.tmp:/tamanu
    - ./.ssh:/root/.ssh
  environment:
    ELECTRON_CONFIG_CACHE: "$HOME/cache/electron"
    CONFIG_SYNC_DB_PATH: "mongodb://mongodb:27017"
  depends_on:
    - mongodb

awsdeployment:
  build:
    dockerfile: Dockerfile.deploy
  encrypted_env_file: codeship.env.encrypted
  volumes:
    - ./.tmp:/tamanu
    - ./.ssh:/root/.ssh

mongodb:
  image: healthcheck/mongo
  ports:
      - 27017:27017
